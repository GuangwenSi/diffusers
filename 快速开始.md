# 🚀 Diffusers 快速开始指南

## 📚 文档导航

- **[项目结构说明.md](./项目结构说明.md)** - 详细的项目结构说明，了解每个模块的作用
- **[安装配置指南.md](./安装配置指南.md)** - 完整的安装和配置步骤

## ⚡ 5分钟快速开始

### 1. 创建虚拟环境

**Windows PowerShell:**
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

**Linux/macOS:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 2. 安装依赖

```bash
pip install --upgrade pip
pip install diffusers[torch] transformers accelerate
```

### 3. 运行第一个示例

```python
from diffusers import DiffusionPipeline
import torch

# 加载模型
pipe = DiffusionPipeline.from_pretrained(
    "runwayml/stable-diffusion-v1-5",
    torch_dtype=torch.float16 if torch.cuda.is_available() else torch.float32
)

# 移动到GPU（如果可用）
if torch.cuda.is_available():
    pipe = pipe.to("cuda")

# 生成图像
image = pipe("a beautiful landscape").images[0]
image.save("output.png")
print("图像已保存到 output.png")
```

## 📖 核心概念

### 三大核心组件

1. **Pipelines（管道）** - 端到端的推理接口
2. **Models（模型）** - 神经网络架构
3. **Schedulers（调度器）** - 控制采样过程

### 典型工作流程

```
加载管道 → 配置参数 → 运行推理 → 获取结果
```

## 🎯 常用任务

### 文本生成图像
```python
from diffusers import StableDiffusionPipeline

pipe = StableDiffusionPipeline.from_pretrained("runwayml/stable-diffusion-v1-5")
image = pipe("a cat wearing a hat").images[0]
```

### 图像到图像
```python
from diffusers import StableDiffusionImg2ImgPipeline

pipe = StableDiffusionImg2ImgPipeline.from_pretrained("runwayml/stable-diffusion-v1-5")
image = pipe(prompt="a cat", image=init_image, strength=0.75).images[0]
```

### 图像修复
```python
from diffusers import StableDiffusionInpaintPipeline

pipe = StableDiffusionInpaintPipeline.from_pretrained("runwayml/stable-diffusion-inpainting")
image = pipe(prompt="a cat", image=image, mask_image=mask).images[0]
```

## 🔧 开发模式安装

如果你想修改源代码或运行示例：

```bash
# 克隆仓库（如果还没有）
git clone https://github.com/huggingface/diffusers.git
cd diffusers

# 安装开发版本
pip install -e ".[dev]"

# 安装示例依赖
cd examples/text_to_image
pip install -r requirements.txt
```

## 📂 项目结构速览

```
E/
├── src/diffusers/     # 核心库代码
│   ├── pipelines/     # 推理管道
│   ├── models/        # 模型架构
│   ├── schedulers/    # 调度器
│   └── utils/         # 工具函数
├── examples/          # 训练和推理示例
├── tests/             # 测试代码
└── docs/              # 文档
```

详细说明请查看 [项目结构说明.md](./项目结构说明.md)

## 🆘 获取帮助

- **官方文档**: https://huggingface.co/docs/diffusers
- **GitHub Issues**: https://github.com/huggingface/diffusers/issues
- **Discord社区**: https://discord.gg/G7tWnz98XR

## ✅ 下一步

1. ✅ 阅读 [安装配置指南.md](./安装配置指南.md) 完成环境配置
2. ✅ 阅读 [项目结构说明.md](./项目结构说明.md) 了解项目架构
3. ✅ 查看 `examples/` 目录下的示例代码
4. ✅ 阅读官方文档深入学习

---

**祝你使用愉快！** 🎉

